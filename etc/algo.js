function solution(k, n, reqs) {
    var answer = 0;
    return answer;
    let mentoArr = (k,n) => {
        let result = [];
        let cnt = n-k;
        let default_mento = Array(k).fill(1);
        console.log(default_mento);
        return default_mento;
    }
    let test_arr = mentoArr(k,n);
}
solution(3,5,[[10, 60, 1], [15, 100, 3], [20, 30, 1], [30, 50, 3], [50, 40, 1], [60, 30, 2], [65, 30, 1], [70, 100, 2]])